name: laraman
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
    sdk: '>=2.7.0 <3.0.0'
dependencies:
    flutter:
        sdk: flutter

    cupertino_icons:
    get: any
    get_storage: ^1.4.0
    firebase_core:
    firebase_auth:
    cloud_firestore:
    qrscan: ^0.2.22
    cached_network_image: ^2.5.0
    flutter_svg:
    google_fonts: ^1.1.2
 
dev_dependencies:
    flutter_test:
        sdk: flutter

flutter:
    uses-material-design: true
    assets:
        - assets/images/        
 